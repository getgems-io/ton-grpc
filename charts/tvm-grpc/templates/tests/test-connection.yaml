apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tvm-grpc.fullname" . }}-test-connection"
  labels:
    {{- include "tvm-grpc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tvm-grpc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
